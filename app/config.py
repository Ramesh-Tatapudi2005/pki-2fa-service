from pathlib import Path

# Where the decrypted seed will be stored inside the container
SEED_PATH = Path("/data/seed.txt")

# Where the private key will be inside the container
PRIVATE_KEY_PATH = Path("/app/student_private.pem")
